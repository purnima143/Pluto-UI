import ProductGrid from "./ProductGrid";

export default {
  title: 'Example/Product/ProductGrid/ProductGrid',
  component: ProductGrid,
  tags: ['autodocs'],
  
};

// Default ProductGrid
export const defaultProductGrid = (args) => {
  return (
    <ProductGrid />
  );
};